import React from 'react';
import {AgGridReact} from '@ag-grid-community/react';

export const App: React.FunctionComponent = () => <AgGridReact containerStyle={{height: 'auto'}} columnDefs={[]} rowData={[]}/>;
